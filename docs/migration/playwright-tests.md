# Матрица переноса Playwright-тестов

Источник: `LabkeeperEditor/src/tests/playwright`.

В исходном наборе найдено 52 активных сценария и 2 сценария, отключенных комментариями. Моки и перехват сетевых запросов в новом наборе не используются. Статус `перенос` означает проверку того же пользовательского результата через production. Статус `условно` означает, что для надежного production-сценария нужны отдельные данные или контролируемое состояние.

Результат переноса: 42 активных исходных сценария входят в основной production-набор, 10 зарегистрированы с явной причиной условного запуска, 2 ранее отключенных сценария сохранены в матрице. Дополнительный smoke-сценарий проверяет чистое открытие анонимного редактора. Всего Playwright обнаруживает 53 теста. Вычислительные тесты запускаются в уникальных временных проектах авторизованного E2E-пользователя, чтобы не расходовать общий анонимный лимит.

Два сценария публичной анонимной компиляции из основного набора имеют реальную production-реализацию, но во время запуска динамически переходят в skipped, если сервер возвращает общий лимит HTTP 425. При отсутствии этого ограничения тесты требуют предусмотренные сценарием успешные ответы.

## Проверочный прогон

Финальный Docker-прогон против production 19 июля 2026 года:

- Google Chrome: 41 passed, 12 skipped, 0 failed.
- Microsoft Edge: 41 passed, 12 skipped, 0 failed.
- После каждого полного прогона: 0 проектов с зарезервированным префиксом.

В 12 skipped входят 9 явно зарегистрированных условных сценариев, 2 публичные компиляции с текущим ответом HTTP 425 и 1 проверка HTTP 401 при истечении сессии, которую production также останавливает ответом HTTP 425.

## Ошибки компиляции

| Исходный тест | Статус | Production-реализация |
| --- | --- | --- |
| `error-test-without-details` | перенос | Ввести выражение с незакрытой кавычкой и проверить реальную ошибку компилятора |
| `error-test-quota` | условно | Нужен безопасный и детерминированный способ исчерпать квоту тестового аккаунта |
| `error-test-single-operator` | перенос | Ввести выражение с одним ожидаемым оператором |
| `error-test-many-operators` | перенос | Ввести выражение с несколькими допустимыми операторами |
| `error-test-variable` | перенос | Обратиться к несуществующей переменной |
| `error-test-function` | перенос | Вызвать несуществующую функцию |

## Графики

Все графики создаются реальным вычислительным кодом. Сформированные моками объекты `statement` не переносятся.

| Исходный тест | Статус | Production-реализация |
| --- | --- | --- |
| `plotly-line` | перенос | Реальный `plot` с линией |
| `plotly-dotted` | перенос | Реальный `plot` с пунктирной линией |
| `plotly-scatter` | перенос | Реальный `plot` с точками |
| `plotly-with-grid` | перенос | График с сеткой |
| `plotly-with-matjax` | перенос | Формулы в заголовке графика |
| `plotly-histogram` | перенос | Несколько гистограмм |
| `plotly-histogram-bar` | перенос | Гистограмма с координатами `x` и `y` |
| `plotly-histogram-single` | перенос | Одна большая выборка |
| `plotly-histogram-two-dims-test` | перенос | Двумерные данные |
| `plotly-histogram-simple-ladder-test` | перенос | Простая возрастающая выборка |
| `plotly-scatter-error` | перенос | Точки с погрешностями |
| `plotly-from-lab` | перенос | Набор данных из лабораторной работы |
| `plotly-bar-with-double-xGrid` | перенос | Неравномерная дробная ось `x` |
| `plotly-bar-with-duplicate-negative-xGrid` | перенос | Повторяющиеся `x` и отрицательные `y` |
| `plotly-with-matjax-longvalues` | отключен в источнике | Не входит в активный объем |

## Публичный и приватный доступ

| Исходный тест | Статус | Production-реализация |
| --- | --- | --- |
| `project-403-test` | перенос | Создать приватный E2E-проект и открыть его в анонимном контексте |
| `project-404-test` | перенос | Открыть случайный UUID, который не существует |
| `public-project-unauth-user-compilation-ok` | перенос, динамически условно при 425 | Создать публичный E2E-проект и скомпилировать анонимно |
| `public-project-unauth-user-compilation-308` | перенос, динамически условно при 425 | Создать публичный проект с файлом и проверить реальный запрет использования файла анонимным пользователем |
| `public-project-different-user-compilation-ok` | условно | Нужен второй аккаунт или стабильный публичный fixture другого пользователя |

## Редактор и проекты

| Исходный тест | Статус | Production-реализация |
| --- | --- | --- |
| `425-display` | условно | Ответ зависит от лимитов анонимного пользователя и не должен искусственно вызываться на production |
| `phystech-icon-test` | условно | Нужен отдельный аккаунт с доменом `phystech.edu` |
| `insert-segment-between` | перенос | Добавление сегментов через верхнее меню и разделители |
| `many-segments` | перенос | Добавление десяти Markdown-сегментов |
| `public-project-test` | перенос | Публичный E2E-проект в режиме чтения |
| `save-test` | перенос | Изменить проект, перезагрузить страницу и проверить сохраненное состояние |
| `ESC-test` | перенос | Закрытие поиска, файлов и проблем клавишей Escape |
| `Search-test` | перенос | Поиск и закрытие через Escape и кнопку очистки |
| `many-errors-test` | перенос | Реальные ошибки с ограниченным безопасным объемом и содержательной проверкой |
| `md2pdf-test` | перенос | Реальная компиляция Markdown с таблицей и блоком кода |
| `rename-project-in-editor-via-enter` | перенос | Переименовать, перезагрузить и проверить новое название |
| `llm-prompt-ok-request-test` | условно | Нужны разрешение на расход токенов и стабильное правило проверки недетерминированного ответа |
| `llm-prompt-bad-request-test` | условно | Нужен реальный ввод, стабильно возвращающий HTTP 400 |
| `rename-project-in-editor-via-press` | перенос | Сохранение названия при потере фокуса |
| `many-segments-move` | перенос | Перемещение сегментов и проверка порядка текста |
| `remove-lines-with-errors-test` | перенос | Реальная ошибка подстановки и три способа удаления текста |
| `default-project-401-test` | условно | Production штатно открывает default-проект анонимно, поэтому реальный HTTP 401 без подмены не воспроизводится |
| `project-401-test` | перенос | Удалить данные сессии и проверить анонимный отказ при повторном открытии приватного проекта |
| `compilation-401-test` | условно | Проверить реальный HTTP 401, если production не остановил анонимный запрос общим лимитом HTTP 425 |
| `file-manager-401-test` | перенос | Удалить данные сессии перед открытием файлового менеджера |
| `compilation-500-test` | условно | Без моков нельзя безопасно и детерминированно заставить production вернуть HTTP 500 |
| `upload-file-413` | перенос | Загрузить файл чуть больше разрешенного лимита и проверить отказ без сохранения |
| `file-list-changes-after-compilation` | перенос | Вызвать `save_csv` и проверить реальный новый файл |
| `double-plots-and-tables-test` | перенос | Дважды скомпилировать измененный вычислительный код |
| `latex-segments-and-asciimath` | отключен в источнике | Не входит в активный объем |
| `oauth2-code-offline-provider` | условно | Сценарий требует искусственного отключения сети, что нарушает требование об отсутствии перехватов |
| `rename-project-via-enter` | перенос | Переименование из списка проектов с проверкой после обновления |
| `rename-project-via-press` | перенос | Переименование из списка проектов при потере фокуса |

## Правила реализации

- В spec-файлах нет `page.locator`, `getByRole`, CSS-селекторов и прямой работы с DOM.
- Все локаторы находятся в `tests/dsl/locators`.
- Все действия и проверки находятся в DSL.
- Подготовка данных работает только с проектами, имя которых начинается с зарезервированного E2E-префикса.
- Сетевые ответы не подменяются, не прерываются и не модифицируются.
- Проверяется итоговое состояние production, а не только факт отправки запроса.
